import React from "react";//
import ReactDOM from "react-dom/client"; //hook our react app to index

import App from "./App";
import "./index.css";

const root=ReactDOM.createRoot(document.getElementById('root'));

root.render(<App />, );
// ^^direction where the app will live, in index 
